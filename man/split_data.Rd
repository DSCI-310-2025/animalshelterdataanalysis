% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_data.R
\name{split_data}
\alias{split_data}
\title{Split a Data Frame into Training and Testing Sets}
\arguments{
\item{df}{A data frame to be split.}

\item{prop}{A numeric value between 0 and 1 specifying the proportion of the data to be used for training (e.g., \code{0.75} for 75\% training data).}

\item{strata}{A string specifying the column name for stratified sampling (must be a column in \code{df}).}

\item{seed}{An integer for reproducibility of the random split.}
}
\value{
A list containing two elements:
\describe{
\item{train}{The training subset of the data.}
\item{test}{The testing subset of the data.}
}
}
\description{
Split a Data Frame into Training and Testing Sets

Uses the \code{initial_split()} function from the \code{rsample} package (via \code{tidymodels}) to split a data frame into training and testing sets.
}
\examples{
\dontrun{
library(tidymodels)
df <- data.frame(
  age = c(2, 3, 4, 5, 6, 7, 8, 9),
  outcome_group = rep(c("Adopted", "Not Adopted"), 4)
)

split <- split_data(df, prop = 0.75, strata = "outcome_group", seed = 123)
train_data <- split[[1]]
test_data <- split[[2]]
}
}
